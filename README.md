## BestProxy Multi-user Telegram Bot

### Setup
1) Copy `.env.example` -> `.env` and set:
- BOT_TOKEN
- FERNET_SECRET
- BESTPROXY_BASE_URL

2) Install:
pip install -r requirements.txt

3) Configure endpoints in `endpoints.py`
(Use provider docs or browser Network tab)

4) Run:
python bot.py
